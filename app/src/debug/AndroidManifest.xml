<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only overlay: testOnly keeps a Device Owner set from this build removable
     via `adb shell dpm remove-active-admin` — without it, undoing DO on a real
     device requires a factory reset. Release builds are unaffected. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:testOnly="true" />
</manifest>
